<?xml version="1.0"?>
<ruleset name="Fearless Living Child Theme">
	<description>WordPress Coding Standards for FLI Child Theme</description>

	<!-- What to scan -->
	<file>.</file>

	<!-- Exclude patterns -->
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/.git/*</exclude-pattern>
	<exclude-pattern>*/assets/*</exclude-pattern>
	<exclude-pattern>*.min.js</exclude-pattern>
	<exclude-pattern>*.min.css</exclude-pattern>

	<!-- Show progress and sniff codes -->
	<arg value="ps"/>
	<arg name="colors"/>
	<arg name="extensions" value="php"/>

	<!-- Use WordPress Coding Standards -->
	<rule ref="WordPress-Core"/>
	<rule ref="WordPress-Docs"/>
	<rule ref="WordPress-Extra"/>

	<!-- Allow for theme specific prefixes -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="fli"/>
				<element value="fearless"/>
			</property>
		</properties>
	</rule>

	<!-- Relax some rules for theme development -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="buddyboss-theme"/>
			</property>
		</properties>
	</rule>

	<!-- PHP version compatibility -->
	<config name="testVersion" value="7.4-"/>

	<!-- Exclude certain rules for development -->
	<rule ref="WordPress.PHP.DevelopmentFunctions">
		<exclude name="WordPress.PHP.DevelopmentFunctions.error_log_error_log"/>
		<exclude name="WordPress.PHP.DevelopmentFunctions.error_log_print_r"/>
	</rule>

	<!-- Allow inline comments without periods -->
	<rule ref="Squiz.Commenting.InlineComment">
		<exclude name="Squiz.Commenting.InlineComment.InvalidEndChar"/>
	</rule>

	<!-- Relax Yoda conditions -->
	<rule ref="WordPress.PHP.YodaConditions">
		<severity>3</severity>
	</rule>
</ruleset>
